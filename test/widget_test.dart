import 'package:flutter_test/flutter_test.dart';

void main() {
  test('package model parses mock-shaped json', () {
    // Smoke placeholder — expand with widget tests after `flutter create .`
    expect(1 + 1, 2);
  });
}
